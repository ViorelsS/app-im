import React from "react";

type Props = {};

export default function Footer({}: Props) {
  return (
    <footer className="h-12 flex items-center justify-center font-semibold shadow-top">
      Made with &hearts; by JE Italy
    </footer>
  );
}
